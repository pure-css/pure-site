<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>{{title}}</title>

    {{addCdnCSS "3.9.0pr3" "cssnormalize" prepend=true}}
    <link rel="stylesheet" href="{{{cdnCSSURL}}}" />

    {{addGithubCSS "tilomitra" "csslist"   "master/css/list.css"}}
    {{!addGithubCSS "tilomitra" "cssextras" "gh-pages/cssextras.css"}}
    {{addGithubCSS "ccampbell" "rainbow"   "master/themes/github.css"}}

  {{#githubCSS}}
    <link rel="stylesheet" href="{{{.}}}" />
  {{/githubCSS}}

    <link rel="stylesheet" href="/css/main.css" />

  {{#if typekit}}
    <script src="http://use.typekit.net/{{typekit}}.js"></script>
    <script>
        try{ Typekit.load(); }catch (e){}
    </script>
  {{/if}}
</head>
<body>

{{> menu}}

{{{body}}}

{{> legal}}

<script src="/vendor/rainbow-min.js"></script>

</body>
</html>
